# 算法议题（待跟进）

> 记录内核层面**已发现、暂不动手**的改进点。经领导确认后按序推进。
> 准则：不动「基石」既有口径，除非该口径本身被证明有误；每次改动必过回归验证
> （`_verify_chan/` 下 `cmp_probe.js` / `batch.js` / `unit_buy2.js` / `v48_edge.py`）。

| 编号 | 议题 | 状态 | 来源 | 备注 |
|---|---|---|---|---|
| A-01 | 一买判定缺「背驰」闸门 | ✅ **已落地 v4.8（2026-09-22）** | 领导周线复盘 | 见下节 |
| A-02 | 段锚点用「固定 240 根」兜底，跨级别不守恒 | 📝 记录在案 | v4.8 落地时暴露 | 见下节 |
| A-03 | 无活动一买时，走势段一买候选被直接画出（与结论卡不一致） | 📝 记录在案（v4.5 起既有） | v4.8 回归对拍 | 见下节 |

---

## A-01　一买判定缺「背驰」闸门 —— ✅ 已落地 v4.8

### 发现（2026-09-22 领导周线复盘）

上证指数**周线**，2026-07-24 那周被判为「一买」。结构上它确实过了原有九道闸门
（跌破中枢 ZD ✓、低于中枢前那一下跌 ✓、是一卖之后最低 ✓）。但把周线两段下跌的
**MACD 绿柱面积**算出来对比：

| 段 | 区间 | MACD 绿柱面积 | 与前一跌笔之比 |
|---|---|---|---|
| 下跌① 笔32 | 03-06 → 03-27 | 110.1 | — |
| 下跌② 笔34 | 05-15 → 07-24 | **346.5** | **3.15 倍** |

**力度不但没衰减，反而是前一段的 3.15 倍** —— 下跌仍在加速，7-24 只是途中的新低，
不可能是趋势终点。而九道闸门**全是结构闸门**，没有任何一条问「力度衰竭了没有」。

### 落地口径（v4.8）

新增第 7 道闸门「**本级别背驰**」：
`背离成立 ⟺ 本笔下跌的 MACD 绿柱面积 < 前一个同向下跌笔的面积`（不设固定阈值）。

- 位置：`chan-core` 的 `evaluate()`（影响 hit / 结论卡 / 活动一买状态机）
  **+ `findZoushiPoints()`（影响图上走势段）** —— 两处必须同口径，否则图上会画着
  「一买」而结论卡写着「未命中」，自相矛盾。
- 新增 helper：`downForce()` / `prevDownIdx()` / `l1DivGate()`；
  `findZoushiPoints(bis, zs, x0, x1, hist)` 增加第 5 个参数 `hist`（MACD 柱数组）。
  **`hist` 缺失时闸门跳过**（保持旧行为），所以全部调用点都必须传。

### ⚠️ 判据选型踩过的坑（必须记住）

第一版用「**面积 ÷ K 线数**（单位力度）」做主判据，实测周线 7-24 卡在刀刃上：

| 计数口径 | 笔32 单位 | 笔34 单位 | 比值 | 结论 |
|---|---|---|---|---|
| 区间长度（不含首尾，3 / 10 周） | 36.70 | 34.65 | **0.94** | 判定"背驰" → 7-24 会漏放 |
| 含首尾（4 / 11 根） | 27.53 | 31.50 | **1.14** | 判定"无背驰" → 拦下 |

**同一段行情，只是计数方式不同，结论直接翻转** —— 这种判据不能用。
换成**面积**（3.15 倍）后结论被稳稳压住，不随计数口径漂移，且与页面既有
「背驰量化」`segPower()` 同源、与缠论原著口径一致。

> 教训：**判据要选「不漂移」的那个**。比值贴近 1 的判据，等于把结论交给实现细节。

### 验证

| 级别 | 本笔面积 | 前一跌笔面积 | 比值 | 结果 |
|---|---|---|---|---|
| 日线 7-20（笔47） | 288.5 | 360.4 | **0.80** | 衰减 → 背驰 → 一买**保留** ✓ |
| 周线 7-24（笔34） | 346.5 | 110.1 | **3.15** | 更强 → 无背驰 → 一买**取消** ✓ |

同一价位 3741.11、同一周，**日线是一买、周线不是** —— 这就是「级别」：
本级别的力度只与本级别的笔比，绝不拿日线的衰减去套周线。

### 未采纳的方案（留档）

「最后两个中枢不重叠（趋势）」曾作为候选硬闸门，**实跑后被否**：
日线 7-20 当时最后两个中枢（中枢13 `4061.82~4114.84` / 中枢14 `4061.82~4175.35`）
同样重叠，加进去会把**正确的一买一起误杀**。该条件仅作观察项，不进闸门。

---

## A-02　段锚点用「固定 240 根」兜底，跨级别不守恒

### 现象

无活动一买时，走势段窗口兜底为 `[bars.length - 240, 末根]`：

| 级别 | 240 根 ≈ | 实测窗口起点 |
|---|---|---|
| 日线 | 1 年 | 合理 |
| 周线 | **4.6 年** | 2022-01-07（上证） |

周线上这个兜底窗口会一直回溯到 4 年前，一卖取到 `2022-01-07` 之后的最高向上笔。
上证恰好在窗口内最高点是 2026-05-15（4258.86），所以结果正确 —— 但这是**运气**，
不是口径保证。换成一只 2021 年有大泡沫顶的票，一卖会被锚到 2021 年。

### 待决

1. 兜底窗口是否改成**按级别的时间跨度**（如日线 ~2 年、周线 ~2 年、月线 ~4 年）？
2. 还是改成「**最近一卖锚定**」：先定位最近一个"后续未被超越"的向上笔高点作段起点？
3. 或直接复用 `/multi/` 已有的 `trendChainStart(zs)`（沿中枢重叠链回退到本段起点）？
   —— 但实测它在长盘整票上会一路退到 0，同样不理想。

### 状态

📝 记录在案。**不在 v4.8 一并改**（领导 2026-09-22：「不要一次性改」）。

---

## A-03　无活动一买时，走势段一买候选被直接画出

`chan-ext` 的 `resolveBuyPoints()` 逻辑是：

```js
if (p.kind === '一买') {
  if (al1 && p.x === al1.idx) { has = true; out.push(p); }   // 有活动一买 → 只认它
  else if (al1) cand.push(...);                              // 有活动一买但不是它 → 藏起来
  else out.push(p);                                          // ★ 无活动一买 → 直接画
  continue;
}
```

最后那条 `else out.push(p)` 是个**遗留缺口**：`findZoushiPoints` 给出的「一买」只是
「跌破最后中枢的向下笔」候选，并没有过闸门；无活动一买时却会被直接画到图上，
于是出现「图上有一买、结论卡说未命中」。

v4.8 回归对拍实测到的例子（**v4.7 就已存在，非本次引入**）：
`sz300750` 一卖@2026-05-07 / 二卖@2026-06-01 / **一买@2026-09-08**，而活动一买为 null。

### 待决

是否收紧为「**无活动一买 → 一买候选一律不画**」（只由结论卡说明）？
影响面需要重新跑 48 只样本评估 —— 属于独立变更，不在 v4.8 一并改。

### 状态

📝 记录在案。v4.5 的注释里已埋了这条线索，本次确认它是真实缺口。

---

## v4.8 全站回归对拍（2026-09-22）

`batch.js` 跑 48 只样本（`_verify_chan/data/`），v4.7 → v4.8：

| 指标 | 数值 |
|---|---|
| 完全一致 | **30 / 48** |
| 有差异 | 18（段重锚 17 + 仅买卖点变化 1） |

差异分两类，都可解释：

1. **背驰闸门生效** —— 其中 **3 只票失去「当下命中」**（`sh000300` 9-16、`sh601012` 9-16、
   `sz300124` 9-16），这些是**不背驰的假一买**，正是本次要滤掉的。另有 1 只（`sz300750`）
   本就没有活动一买，但图上原有的 `一买@2026-09-08` 被闸门撤掉。
2. **段重锚** —— **17 只票**的一卖/二卖日期前移或后移。根因：一买被判失效后，
   走势段锚点从「旧一买」改挂到「最近的一卖」，属 A-02 提到的那套兜底窗口。
   **这是为了让周线能看到 9-04 二卖而必须付的代价**，逐一核对后未发现逻辑错误，
   但若领导认为段锚点不宜大动，可按 A-02 单独返工。

**没有一只票新增「当下命中」**（无假阳性），方向是单向收紧。

全站哈希（改后）：`chan-core` = `f22f68da4300`，`chan-ext` = `ef48bc2474cf`。

---

*本文件由紫坠维护；新增议题请追加表格行 + 补一节明细。*
